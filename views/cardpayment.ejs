<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Card Payment</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-dark text-white">

<div class="container mt-5">

    <h2 class="mb-4">Credit Card Payment</h2>

    <form action="/payment/card/confirm/<%= paymentID %>" method="POST" class="bg-secondary p-4 rounded">

        <div class="mb-3">
            <label>Card Number</label>
            <input type="text" class="form-control" name="cardNumber" maxlength="16" required>
        </div>

        <div class="mb-3">
            <label>Expiry Date</label>
            <input type="text" class="form-control" name="expiry" placeholder="MM/YY" required>
        </div>

        <div class="mb-3">
            <label>CVV</label>
            <input type="password" class="form-control" name="cvv" maxlength="3" required>
        </div>

        <button class="btn btn-success w-100">Confirm Payment</button>
    </form>

    <a href="/payment/<%= paymentID %>" class="btn btn-secondary mt-3">Back</a>

</div>

</body>
</html>
