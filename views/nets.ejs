<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>NETS QR Payment</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-dark text-white">

    <div class="container mt-5 text-center">

        <h2 class="mb-4">NETS QR Payment</h2>

        <p class="text-light">Scan the NETS QR code below to complete your payment.</p>

        <div class="bg-white p-4 d-inline-block rounded shadow my-3">
            <img
              src="https://chart.googleapis.com/chart?cht=qr&chs=260x260&chl=NETS%20Payment%20ID%3A%20<%= paymentID %>%20Amount%3A%20$<%= amount.toFixed(2) %>"
              width="260"
              height="260"
              alt="NETS QR"
            >
        </div>

        <h4 class="mt-3 text-success">Amount to Pay: $<%= amount.toFixed(2) %></h4>

        <form action="/payment/nets/confirm/<%= paymentID %>" method="POST" class="mt-4">
            <button class="btn btn-success btn-lg">I Have Paid</button>
        </form>

        <a href="/payment/<%= paymentID %>" class="btn btn-secondary mt-3">
            Back to Payment Options
        </a>

    </div>

</body>
</html>
